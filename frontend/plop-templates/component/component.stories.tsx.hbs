import type { Meta, StoryObj } from "@storybook/react";

import { {{pascalCase name}}Presenter } from "./{{pascalCase name}}";

type T = typeof {{pascalCase name}}Presenter;

const meta = {
  title: "{{pascalCase name}}",
  component: {{pascalCase name}}Presenter,
  parameters: {
    layout: "centered",
  },
  tags: ["autodocs"],
  argTypes: {},
} satisfies Meta<T>;

export default meta;

export const Default: StoryObj<T> = {};
